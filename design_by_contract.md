# `_candidate_entry_points` / `_candidate_speed` / `_candidate_distance` branch contract

Branch-by-branch comparison of `AbstractRailEnv._candidate_entry_points()`, `_candidate_speed()` and
`_candidate_distance()` (`flatland/envs/rail_env.py`). All three are pure, pre-step-only derivations of this
step's optimistic candidate entry points/speed/distance, called from the collect phase and cross-checked by the
post-step invariant checks. `_candidate_entry_points` is the *producer*: it derives `is_off_map`/`is_cell_exit`/
`target_reached`/`map_entry` from scratch and returns the resolved `candidate_entry_point`. `_candidate_speed`/
`_candidate_distance` are *consumers*: they take that resolved `candidate_entry_point` as an input parameter
rather than re-deriving it. That asymmetry is why some terms/branches can be made textually identical across all
three, and others structurally can't - noted case by case below.

Every branch condition is written to be self-contained (order-independent), and for every case shared between
`_candidate_speed`/`_candidate_distance`, textually identical. `_candidate_entry_points`' shared branches
(Done/Malfunction/Map entry/Stay off map/Invalid action at cell exit) use the same exclusion structure and
branch order as the other two; its own Target reached/Map entry/On-map cell transition necessarily keep their
own richer, from-scratch derivations.

## Table 1 — case / formula / candidate value

| Case                             | Full formula                                                                                                                                                                                        | `_candidate_entry_points`                                                                                             | `_candidate_speed`                                                                                                                                                                                                                                       | `_candidate_distance`                                                                                                                                                              |
|----------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Done**                         | `pre_done`                                                                                                                                                                                          | `(pre_current_entry_point, pre_next_entry_point)`                                                                     | `0`                                                                                                                                                                                                                                                      | `distance_without_crossing(offset, speed)`                                                                                                                                         |
| **Target reached**               | `target_reached ∧ ¬pre_done` (same shared `target_reached` formula in all three, see Table 2b)                                                                                                       | `(pre_next_entry_point, candidate_entry_point_independent)`                                                           | `0`                                                                                                                                                                                                                                                      | `None` if `remove_agents_at_target` else `distance_without_crossing(offset, speed)`                                                                                                |
| **Malfunction**                  | `in_malfunction ∧ ¬pre_done ∧ ¬target_reached`                                                                                                                                                      | `(pre_current_entry_point, pre_next_entry_point)`                                                                     | `0`                                                                                                                                                                                                                                                      | `pre_offset`                                                                                                                                                                       |
| **Map entry**                    | entry_points: `map_entry ∧ ¬pre_done ∧ ¬in_malfunction`; speed/distance: `is_off_map ∧ candidate≠None ∧ ¬pre_done ∧ ¬target_reached ∧ ¬in_malfunction`                                              | `(agent.initial_entry_point, candidate_entry_point_independent)`                                                      | `cap_speed(max_speed, accel_delta)`                                                                                                                                                                                                                      | `0`                                                                                                                                                                                |
| **Stay off map**                 | entry_points: `off_map_no_departure ∧ ¬pre_done ∧ ¬in_malfunction`; speed/distance: `is_off_map ∧ stay_off_map ∧ ¬pre_done ∧ ¬target_reached ∧ ¬in_malfunction` (see Table 2b)                       | `(pre_current_entry_point, pre_next_entry_point)` = `(None, None)`                                                    | `0`                                                                                                                                                                                                                                                      | `None`                                                                                                                                                                             |
| **On-map cell transition**       | `on_map_cell_transition ∧ ¬pre_done ∧ ¬in_malfunction ∧ ¬target_reached`                                                                                                                            | `(pre_next_entry_point, candidate_entry_point_independent)`                                                           | entry_points-only branch — absorbed into speed's **Default**                                                                                                                                                                                             | entry_points-only branch — absorbed into distance's **Default**                                                                                                                    |
| **Invalid action at cell exit**  | `action_invalid_on_rail ∧ is_cell_exit ∧ ¬pre_done ∧ ¬target_reached ∧ ¬in_malfunction ∧ ¬is_off_map` (same terms and the same `is_cell_exit` formula in all three, see Table 2a) | `(pre_current_entry_point, pre_next_entry_point)` (unchanged - denied)                                                | `0`                                                                                                                                                                                                                                                      | `distance_without_crossing(offset, speed)`                                                                                                                                         |
| **Stopped** (`stopped`)          | distance only, dedicated branch — entry_points doesn't distinguish by `pre_speed`/action at all                                                                                                     | via **On-map cell transition** if `is_cell_exit ∧` valid action, else via **Default** — same as every other row below | `pre_speed` (=`0`) *(via Default, if `DO_NOTHING`)*; `speed_after_acceleration(0, max_speed, accel_delta)` *(via Acceleration/start moving, if a moving action given)*; `speed_after_braking(0, braking_delta)` (=`0`) *(via Braking, if `STOP_MOVING`)* | `pre_offset`                                                                                                                                                                       |
| **Acceleration or start moving** | speed only, dedicated branch                                                                                                                                                                        | via **On-map cell transition**/**Default** (see above)                                                                | `speed_after_acceleration(pre_speed, max_speed, accel_delta)`                                                                                                                                                                                            | `pre_offset` *(via Stopped, if `stopped` — the "start moving" sub-case)*; `distance_after_crossing(offset, speed)` *(via Default, if `¬stopped` — already moving, `MOVE_FORWARD`)* |
| **Braking**                      | speed only, dedicated branch                                                                                                                                                                        | via **On-map cell transition**/**Default** (see above)                                                                | `speed_after_braking(pre_speed, braking_delta)`                                                                                                                                                                                                          | `pre_offset` *(via Stopped, if `stopped`)*; `distance_after_crossing(offset, speed)` *(via Default, if `¬stopped` — braking doesn't halt an already-in-flight boundary crossing)*  |
| **Default**                      | on map, mid-cell (`¬is_cell_exit`) - narrower than speed/distance's Default, which also absorbs **On-map cell transition**                                                                          | `(pre_current_entry_point, pre_next_entry_point)` (unchanged - genuinely mid-cell)                                    | `pre_speed`                                                                                                                                                                                                                                              | `distance_after_crossing(offset, speed)`                                                                                                                                           |

`Stopped`/`Acceleration or start moving`/`Braking` are not a clean fusion between speed and distance: distance
partitions this region by physical state (`stopped` or not), speed partitions the same region by the
action given that step. Neither is a subset of the other - the two methods reach the same practical outcomes
via different, non-corresponding branches there. `_candidate_entry_points` doesn't partition this region at
all - it only cares whether a genuine crossing is happening (`is_cell_exit` + a valid action), which is
orthogonal to both speed's and distance's split.

Since `is_cell_exit` requires `pre_speed > 0` (see Table 2a), `invalid_action_at_cell_exit` can never be true
while `stopped` - a `STOPPED` agent given a moving action always falls through **Invalid action at cell exit**
into **Acceleration or start moving**, regardless of whether that action is itself structurally valid (e.g. a
non-L/R action at a symmetric switch). This is intentional: it is what lets a `STOPPED` agent banked at a
switch boundary optimistically resume `MOVING` on any moving-action retry, exactly like an ordinary
resource-check-blocked agent already does elsewhere - the *next* step then genuinely re-attempts the crossing
(now with `pre_speed > 0`), and is denied and re-charged again if the action is still invalid. The penalty is
per genuine entering attempt, not per retry (see `BaseDefaultRewards.step_reward` in `rewards.py`).

## Table 2a — preconditions

Base terms: none of these reference any other term in Table 2a/2b - each is a direct fact about the
pre-step state, the action given, or the rail topology.

| Term                                                          | Definition                                                                                                                                                                                                                                                                                                                                            | Same across all three?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
|----------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `pre_done`                                                     | `agent.target_entry_point is not None` *(via PreStepSnapshot for the post-step checks)*                                                                                                                                                                                                                                                              | ✅ identical                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `in_malfunction`                                               | `agent.malfunction_handler.in_malfunction` *(via PreStepSnapshot for the post-step checks)*                                                                                                                                                                                                                                                          | ✅ identical                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `is_off_map`                                                   | `pre_current_entry_point is None`, where `pre_current_entry_point = agent.current_entry_point` *(via PreStepSnapshot for the post-step checks)*                                                                                                                                                                                                      | ✅ identical - all three read the same variable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `action_invalid_on_rail`                                       | `candidate_entry_point_independent is None`, where `candidate_entry_point_independent = self.rail.apply_action_independent(RailEnvActions.from_value(action_dict.get(agent.handle, RailEnvActions.DO_NOTHING)), agent.next_entry_point if agent.next_entry_point is not None else agent.initial_entry_point)` *(via PreStepSnapshot, all call paths)* | ✅ identical - a named local in all three, computed the same way from the `candidate_entry_point_independent` parameter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `is_cell_exit`                                                 | `pre_speed is not None and pre_speed > 0 and pre_offset + pre_speed >= SEGMENT_LENGTH`                                                                                                                                                                                                                                                               | ✅ identical - one shared definition across all three `_candidate_` methods, and also `SpeedCounter.is_cell_exit()`/`_cached_cell_exit()` (`step_utils/speed_counter.py`), which live per-step code (e.g. `action_valid` in `step()`'s collect loop) consults directly. The `pre_speed > 0` guard is load-bearing everywhere, not just in entry_points: without it, a `STOPPED` agent (`pre_speed == 0`) parked exactly at a boundary would both (a) be misclassified by entry_points as completing a crossing/reaching target this step, since entry_points alone decides whether the agent's *position* actually changes, and (b) never be allowed to resume `MOVING` on a retry (see the note under Table 1) - `SpeedCounter.is_cell_exit()` reading `False` at speed 0 is what lets `action_valid` become `True` again for a banked, `STOPPED` agent. |
| `movement_action_given`                                        | `RailEnvActions.is_moving_action(action)` (entry_points only)                                                                                                                                                                                                                                                                                        | N/A                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ready_to_depart`                                              | `agent.earliest_departure == 0` if `elapsed_steps == 1` else `agent.earliest_departure <= elapsed_steps` (entry_points only)                                                                                                                                                                                                                         | N/A                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `stopped`                                                      | `pre_speed == 0`, where `pre_speed = agent.speed_counter.speed` *(via PreStepSnapshot for the post-step checks)*                                                                                                                                                                                                                                     | ✅ identical expression - named local in both `_candidate_speed` (used in its Acceleration or start moving branch) and `_candidate_distance` (used as its Stopped branch's own guard); entry_points doesn't branch on it directly                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

## Table 2b — consequences

Derived terms: each one's definition composes at least one term from Table 2a, or from earlier in this
table. Rows are ordered by dependency: a term used in a later row's definition is always defined in an
earlier row (Table 2a first, then this table top to bottom).

| Term                                                          | Definition                                                                                                                                                                                                                                                                                                                                            | Same across all three?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
|----------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `target_reached`                                               | `not is_off_map and is_cell_exit and pre_next_entry_point in agent_targets`, where `pre_next_entry_point` is a plain parameter of `_candidate_speed`/`_candidate_distance` too, not only `_candidate_entry_points`.                                                                                                                                  | ✅ identical - one shared definition in all three, read directly from `pre_next_entry_point` rather than by checking set membership on the already-resolved `candidate_entry_point`. Requiring `not is_off_map and is_cell_exit` structurally guarantees target-reached only fires for an agent genuinely completing a crossing into a target cell - not any other coincidence of position and target-set membership (e.g. an off-map agent's `initial_entry_point` happening to coincide with a target on the step it departs).                                                                                                                                                                                                                                                                                                                         |
| `on_map_cell_transition`                                       | `not is_off_map and is_cell_exit and not action_invalid_on_rail` (entry_points only) - target_reached's on-map counterpart: a genuine crossing that isn't (also) a target arrival                                                                                                                                                                    | N/A - entry_points only; absorbed into speed/distance's own **Default** branch (see Table 1), which doesn't need to distinguish a genuine crossing from an ordinary mid-cell step                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `invalid_action_at_cell_exit`                                  | `action_invalid_on_rail and is_cell_exit` (speed/distance name this; entry_points inlines the same conjunction into its Invalid action at cell exit branch)                                                                                                                                                                                          | ✅ same conjunction, using the same shared `is_cell_exit` everywhere                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `required_action_invalid_or_not_required_or_no_movement`       | `action_invalid_on_rail or not movement_action_given or not ready_to_depart` (entry_points only) - names *why* an off-map agent doesn't depart this step, as one of three disjoint cases (see below the table)                                                                                                                                       | N/A - entry_points only, used in place of `not map_entry` in its own off_map_no_departure branch (given `is_off_map`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `map_entry`                                                    | `is_off_map and not required_action_invalid_or_not_required_or_no_movement` (entry_points only - speed/distance consume its *result*, `not stay_off_map`, rather than this richer predicate)                                                                                                                                                        | N/A - speed/distance's "map entry" condition is a downstream proxy for this, not the same expression                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `off_map_no_departure`                                         | `is_off_map and required_action_invalid_or_not_required_or_no_movement` (entry_points only); equivalently `is_off_map and not map_entry`. Core predicate, like `map_entry` above - `not pre_done`/`not in_malfunction` are applied separately at the branch, not folded into this term. Names the branch this way: an agent that was already off map and, for one of `required_action_invalid_or_not_required_or_no_movement`'s three reasons, doesn't depart this step either | N/A - entry_points only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `stay_off_map`                                                 | `candidate_entry_point is None` - a named local in `_candidate_speed`/`_candidate_distance` (used in their own map entry/stay off map branches in place of `candidate_entry_point is not None`/`is None`), consuming the already-resolved value rather than re-deriving it. `None` in exactly three of `_candidate_entry_points`' branches, all requiring `is_off_map`: **Done** (`pre_done` - an already-removed agent stays removed), **Malfunction** (`in_malfunction ∧ ¬pre_done` - `MALFUNCTION_OFF_MAP`), and **off_map_no_departure** (`¬pre_done ∧ ¬in_malfunction ∧ off_map_no_departure`)                          | N/A for entry_points itself (it's the producer of `candidate_entry_point`, not a consumer of this named-local form of it). Distinct in meaning from `action_invalid_on_rail`: `stay_off_map` means "no departure happened this step" - broader than "the action was invalid" (see `required_action_invalid_or_not_required_or_no_movement` above for the full breakdown of just the third of its three producing branches).                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `no_earlier_case_applies`                                      | `¬pre_done ∧ ¬target_reached ∧ ¬in_malfunction ∧ ¬is_off_map ∧ ¬invalid_action_at_cell_exit`                                                                                                                                                                                                                                                          | ✅ same 5 exclusions in all three (speed names it explicitly; distance/entry_points reach the same set via elimination/fallthrough)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

```
stay_off_map (candidate_entry_point is None)
⟺ is_off_map ∧ (
pre_done                                              -- Done
∨ (¬pre_done ∧ in_malfunction)                        -- Malfunction (MALFUNCTION_OFF_MAP)
∨ (¬pre_done ∧ ¬in_malfunction ∧ off_map_no_departure) -- off_map_no_departure
)
```


`required_action_invalid_or_not_required_or_no_movement` breaks down into three disjoint cases - why an
off-map agent doesn't depart this step:

- **not yet ready to depart** (`¬ready_to_depart`) - state not yet `READY_TO_DEPART`, an action isn't
  required yet.
- **ready to depart but no moving action given** (`ready_to_depart ∧ ¬movement_action_given`) - e.g.
  `DO_NOTHING`, an action was required but a departure wasn't attempted.
- **a moving action given that doesn't resolve to a valid transition on the rail**
  (`ready_to_depart ∧ movement_action_given ∧ action_invalid_on_rail`) - an action was required,
  attempted, and invalid.

## Table 3 — distribute phase: candidate accepted or discarded

Tables 1, 2a and 2b are the *collect* phase: purely optimistic, pre-step-only derivations, computed for every agent before
any conflict is resolved. `RailEnv.step()`'s distribute phase then resolves conflicts via `MotionCheck`
(`resource_check = self.resource_check.check_resource(i_agent)`) and, per agent, either commits the already-computed
`candidate_entry_point`/`candidate_next_entry_point`/`candidate_speed`/`candidate_distance` as-is or falls back to a
discarded-candidate formula - this is `step()`'s (10a) POSITION UPDATE and (10b) SPEED_COUNTER UPDATE blocks.
`agent.old_entry_point` is this step's stable pre-step snapshot (captured before (10a) touches anything) of
`pre_current_entry_point`, i.e. `is_off_map` (Table 2a) - (10b)'s own discarded-candidate branch reads it to tell
"was this agent on or off map going into this step" apart: `pass` (a true no-op, not just an unchanged-value
assignment) is only correct off map because the `speed_counter` invariant already holds `(None, None)` there, with
nothing to reset.

| Case                          | Condition                                                                                                                              | Speed (`speed_counter.speed`)                                                           | Distance (`speed_counter.distance`)                                                                                                                                                                | Entry points (`current_entry_point`, `next_entry_point`)                                                                                                                                                                          |
|-------------------------------|----------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Accepted, ends up on map**  | `resource_check ∧ candidate_entry_point is not None ∧ ¬(remove_agents_at_target ∧ (pre_done ∨ candidate_entry_point ∈ agent.targets))` | `candidate_speed`, accepted as-is                                                       | `candidate_distance`, accepted as-is                                                                                                                                                               | `(candidate_entry_point, candidate_next_entry_point)`, accepted as-is - covers every remaining case: a genuine crossing, a mid-cell no-op, map entry, and target reached while parked or `DONE` (`remove_agents_at_target=False`) |
| **Accepted, ends up off map** | `resource_check ∧ (candidate_entry_point is None ∨ (remove_agents_at_target ∧ (pre_done ∨ candidate_entry_point ∈ agent.targets)))`    | `None` - forced (`_candidate_speed` itself never returns `None`, see its own docstring) | `candidate_distance`, accepted as-is - always `None` here by construction (Table 1's Target reached branch, when `remove_agents_at_target`, and its Stay off map branch both always return `None`) | `(None, None)` - either explicitly cleared (target reached and removed) or accepted as-is, since `candidate_entry_point`/`candidate_next_entry_point` are already `(None, None)` (stayed off map)                                 |
| **Denied on-map crossing**    | `¬resource_check ∧ ¬is_off_map`                                                                                                        | `0` - forced stop                                                                       | `distance_without_crossing(pre_offset, pre_speed)` - banked at the boundary, not credited with the (denied) crossing                                                                               | unchanged - `(pre_current_entry_point, pre_next_entry_point)`, candidate discarded                                                                                                                                                |
| **Denied map entry**          | `¬resource_check ∧ is_off_map`                                                                                                         | unchanged - already `None` (off-map invariant)                                          | unchanged - already `None`                                                                                                                                                                         | unchanged - already `(None, None)`. A true no-op: candidate discarded before ever leaving the map                                                                                                                                 |

**Accepted, ends up off map** merges two underlying branches (target reached and removed vs. stayed off map) that
have distinct conditions but always resolve to the identical `(None, None, (None, None))` triple: `_candidate_speed`
never returns `None` itself - it returns `0` for both underlying branches (Table 1) - so (10b) forces `None` in
both, overriding the already-computed `agent_transition_data.candidate_speed`, to match the invariant that a
removed/off-map agent's speed must be `None`. Distance needs no such forcing, since `_candidate_distance` already
returns `None` for both directly (Table 1).
