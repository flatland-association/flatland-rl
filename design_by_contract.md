# `_candidate_entry_points` / `_candidate_speed` / `_candidate_distance` branch contract

Design by Contract (DbC) is a software correctness discipline introduced by Bertrand Meyer in the mid-1980s with
the Eiffel programming language, addressing the problem of specifying precisely - via preconditions,
postconditions and invariants - what a caller must guarantee before invoking a routine and what the routine
guarantees in return, so a broken assumption is caught exactly where it occurs rather than surfacing later as an
unrelated-looking symptom.

Branch-by-branch comparison of `AbstractRailEnv._candidate_entry_points()`, `_candidate_speed()` and
`_candidate_distance()` (`flatland/envs/rail_env.py`). All three are pure, pre-step-only derivations of this
step's optimistic candidate entry points/speed/distance, called from the collect phase and cross-checked by the
post-step postcondition checks. `_candidate_entry_points` is the *producer*: it derives `off_map`/`cell_exit`/
`target_reached`/`map_entry` from scratch and returns the resolved `candidate_entry_point`. `_candidate_speed`/
`_candidate_distance` are *consumers*: they take that resolved `candidate_entry_point` as an input parameter
rather than re-deriving it. That asymmetry is why some terms/branches can be made textually identical across all
three, and others structurally can't - noted case by case below.

Every branch condition is written to be self-contained (order-independent), and for every case shared between
`_candidate_speed`/`_candidate_distance`, textually identical. `_candidate_entry_points`' shared branches
(Done/Malfunction/Map entry/Stay off map/Invalid action at cell exit) use the same exclusion structure and
branch order as the other two; its own Target reached/Map entry/On-map cell transition necessarily keep their
own richer, from-scratch derivations.

## Table 1 — case / formula / candidate value

| Case                             | Full formula                                                                                                                                                            | `_candidate_entry_points`                                                                                          | `_candidate_speed`                                                                                                                                                                                                                                   | `_candidate_distance`                                                                                                                                                              |
|----------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Done**                         | `done`                                                                                                                                                                  | `(current_entry_point, next_entry_point)`                                                                          | `0`                                                                                                                                                                                                                                                  | `distance_without_crossing(distance, speed)`                                                                                                                                       |
| **Target reached**               | `target_reached ∧ ¬done` (same shared `target_reached` formula in all three, see Table 2b)                                                                              | `(next_entry_point, candidate_entry_point_independent)`                                                            | `0`                                                                                                                                                                                                                                                  | `None` if `remove_agents_at_target` else `distance_without_crossing(distance, speed)`                                                                                              |
| **Malfunction**                  | `in_malfunction ∧ ¬done ∧ ¬target_reached`                                                                                                                              | `(current_entry_point, next_entry_point)`                                                                          | `0`                                                                                                                                                                                                                                                  | `distance`                                                                                                                                                                         |
| **Map entry**                    | entry_points: `map_entry ∧ ¬done ∧ ¬in_malfunction`; speed/distance: `off_map ∧ candidate≠None ∧ ¬done ∧ ¬target_reached ∧ ¬in_malfunction`                             | `(agent.initial_entry_point, candidate_entry_point_independent)`                                                   | `cap_speed(max_speed, accel_delta)`                                                                                                                                                                                                                  | `0`                                                                                                                                                                                |
| **Stay off map**                 | entry_points: `off_map_no_departure ∧ ¬done ∧ ¬in_malfunction`; speed/distance: `off_map ∧ stay_off_map ∧ ¬done ∧ ¬target_reached ∧ ¬in_malfunction` (see Table 2b)     | `(current_entry_point, next_entry_point)` = `(None, None)`                                                         | `0`                                                                                                                                                                                                                                                  | `None`                                                                                                                                                                             |
| **On-map cell transition**       | `on_map_cell_transition ∧ ¬done ∧ ¬in_malfunction ∧ ¬target_reached`                                                                                                    | `(next_entry_point, candidate_entry_point_independent)`                                                            | entry_points-only branch — absorbed into speed's **Default**                                                                                                                                                                                         | entry_points-only branch — absorbed into distance's **Default**                                                                                                                    |
| **Invalid action at cell exit**  | `action_invalid_on_rail ∧ cell_exit ∧ ¬done ∧ ¬target_reached ∧ ¬in_malfunction ∧ ¬off_map` (same terms and the same `cell_exit` formula in all three, see Table 2a/2b) | `(current_entry_point, next_entry_point)` (unchanged - denied)                                                     | `0`                                                                                                                                                                                                                                                  | `distance_without_crossing(distance, speed)`                                                                                                                                       |
| **Stopped** (`stopped`)          | distance only, dedicated branch — entry_points doesn't distinguish by `speed`/action at all                                                                             | via **On-map cell transition** if `cell_exit ∧` valid action, else via **Default** — same as every other row below | `speed` (=`0`) *(via Default, if `DO_NOTHING`)*; `speed_after_acceleration(0, max_speed, accel_delta)` *(via Acceleration/start moving, if a moving action given)*; `speed_after_braking(0, braking_delta)` (=`0`) *(via Braking, if `STOP_MOVING`)* | `distance`                                                                                                                                                                         |
| **Acceleration or start moving** | speed only, dedicated branch                                                                                                                                            | via **On-map cell transition**/**Default** (see above)                                                             | `speed_after_acceleration(speed, max_speed, accel_delta)`                                                                                                                                                                                            | `distance` *(via Stopped, if `stopped` — the "start moving" sub-case)*; `distance_after_crossing(distance, speed)` *(via Default, if `¬stopped` — already moving, `MOVE_FORWARD`)* |
| **Braking**                      | speed only, dedicated branch                                                                                                                                            | via **On-map cell transition**/**Default** (see above)                                                             | `speed_after_braking(speed, braking_delta)`                                                                                                                                                                                                          | `distance` *(via Stopped, if `stopped`)*; `distance_after_crossing(distance, speed)` *(via Default, if `¬stopped` — braking doesn't halt an already-in-flight boundary crossing)*  |
| **Default**                      | on map, mid-cell (`¬cell_exit`) - narrower than speed/distance's Default, which also absorbs **On-map cell transition**                                                 | `(current_entry_point, next_entry_point)` (unchanged - genuinely mid-cell)                                         | `speed`                                                                                                                                                                                                                                              | `distance_after_crossing(distance, speed)`                                                                                                                                         |

`Stopped`/`Acceleration or start moving`/`Braking` are not a clean fusion between speed and distance: distance
partitions this region by physical state (`stopped` or not), speed partitions the same region by the
action given that step. Neither is a subset of the other - the two methods reach the same practical outcomes
via different, non-corresponding branches there. `_candidate_entry_points` doesn't partition this region at
all - it only cares whether a genuine crossing is happening (`cell_exit` + a valid action), which is
orthogonal to both speed's and distance's split.

Since `cell_exit` requires `speed > 0` (see Table 2a), `invalid_action_at_cell_exit` can never be true
while `stopped` - a `STOPPED` agent given a moving action always falls through **Invalid action at cell exit**
into **Acceleration or start moving**, regardless of whether that action is itself structurally valid (e.g. a
non-L/R action at a symmetric switch). This is intentional: it is what lets a `STOPPED` agent banked at a
switch boundary optimistically resume `MOVING` on any moving-action retry, exactly like an ordinary
resource-check-blocked agent already does elsewhere - the *next* step then genuinely re-attempts the crossing
(now with `speed > 0`), and is denied and re-charged again if the action is still invalid. The penalty is
per genuine entering attempt, not per retry (see `BaseDefaultRewards.step_reward` in `rewards.py`).

## Branch order (as they appear in code)

The branch labels below are the code comments immediately above each `if` in
`flatland/envs/rail_env.py`, in on-disk order - a quick index into Table 1's rows as they're actually
laid out method by method. Per the note at the top of each method's own branch section, this order is
cosmetic (every branch condition is self-contained and pairwise disjoint - see the branches themselves
for the exclusion reasoning), not load-bearing.

```
_candidate_entry_points          _candidate_speed                 _candidate_distance
------------------------         ------------------------         ------------------------
done                             done                              done
off_map_no_departure             stay off map                      stay off map
keep moving mid-cell             keep moving mid-cell              stopped
on-map cell transition           acceleration or start moving      malfunction
malfunction                      braking                           map entry
map entry                        malfunction                       target reached
target reached                   map entry                         invalid action at cell exit
invalid action at cell exit      target reached
                                 invalid action at cell exit
```

Reordered by empirical branch frequency (`5a19eaf9`): `off_map_no_departure`/`stay off map` is the
single largest branch across every profiled scenario (42-87% of calls), so it's checked right after
`done` rather than last among the off-map-only branches - purely a performance reordering, not a
semantic one (see the note above: every condition is self-contained, so this order is cosmetic).

Each method's final branch (`keep moving mid-cell`) is an explicit `if`, not an implicit `else` - its
condition is the exact logical complement of every branch above it, followed by a
`raise ValueError(...)` that is unreachable by construction (the branches are exhaustive as well as
disjoint).

## Table 2a — preconditions

Base terms: none of these reference any other term in Table 2a/2b - each is a direct fact about the
pre-step state, the action given, or the rail topology.

| Term                    | Definition                                                                                                                                                                                                                                                                                                                                            | Same across all three?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
|-------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `done`                  | `agent.target_entry_point is not None` *(via PreStepSnapshot for the post-step checks)*                                                                                                                                                                                                                                                               | ✅ identical                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `in_malfunction`        | `agent.malfunction_handler.in_malfunction` *(via PreStepSnapshot for the post-step checks)*                                                                                                                                                                                                                                                           | ✅ identical                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `off_map`               | `current_entry_point is None`, where `current_entry_point = agent.current_entry_point` *(via PreStepSnapshot for the post-step checks)*                                                                                                                                                                                                               | ✅ identical - all three read the same variable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `transition_invalid`    | `candidate_entry_point_independent is None`, where `candidate_entry_point_independent = self.rail.apply_action_independent(RailEnvActions.from_value(action_dict.get(agent.handle, RailEnvActions.DO_NOTHING)), agent.next_entry_point if agent.next_entry_point is not None else agent.initial_entry_point)` *(via PreStepSnapshot, all call paths)* | ✅ identical - a named local in all three, computed the same way from the `candidate_entry_point_independent` parameter. Raw fact, on- or off-map: see `action_invalid_on_rail` in Table 2b for the on-map-scoped form.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `cell_exit`             | `cached_cell_exit(max_speed, speed, distance)` (`step_utils/speed_counter.py`) - `True` if `distance is None` (off map), else `speed > 0 and distance + speed >= SEGMENT_LENGTH` (after capping `speed` to `[0, max_speed]`), skipped entirely off map via a fast-path shortcut that returns the same value without paying for the call               | ✅ identical - one shared implementation (`cached_cell_exit`), computed once in `collect()`'s hoisted-flag block and its two postcondition-check mirrors, and by `SpeedCounter.is_cell_exit()` itself (`action_valid` in `step()`'s collect loop consults `is_cell_exit()` directly, not `cell_exit`) - no independent reimplementation anywhere. `cell_exit` itself is only passed as a parameter to `_candidate_entry_points` - `_candidate_speed`/`_candidate_distance` don't take it as a parameter at all (dead there; removed), only the already-derived `target_reached`/`invalid_action_at_cell_exit` computed from it. `cell_exit` returning `True` off map is never read bare off map by any consumer: `_candidate_entry_points`'s two direct reads of it (`on_map_cell_transition`, the keep-moving-mid-cell branch) already conjoin `off_map`/`not off_map` explicitly - so this convention choice never changes a branch's outcome. The `speed > 0` guard is load-bearing on map: without it, a `STOPPED` agent (`speed == 0`) parked exactly at a boundary would both (a) be misclassified by entry_points as completing a crossing/reaching target this step, since entry_points alone decides whether the agent's *position* actually changes, and (b) never be allowed to resume `MOVING` on a retry (see the note under Table 1) - `is_cell_exit()` reading `False` at speed 0 is what lets `action_valid` become `True` again for a banked, `STOPPED` agent. |
| `movement_action_given` | `RailEnvActions.is_moving_action(action)` (entry_points only)                                                                                                                                                                                                                                                                                         | N/A                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `stopped`               | `speed == 0`, where `speed = agent.speed_counter.speed` *(via PreStepSnapshot for the post-step checks)*                                                                                                                                                                                                                                              | ✅ identical expression - named local in both `_candidate_speed` (used in its Acceleration or start moving branch) and `_candidate_distance` (used as its Stopped branch's own guard); entry_points doesn't branch on it directly                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

## Table 2b — consequences

Derived terms: each one's definition composes at least one term from Table 2a, or from earlier in this
table. Rows are ordered by dependency: a term used in a later row's definition is always defined in an
earlier row (Table 2a first, then this table top to bottom).

| Term                                                     | Definition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Same across all three?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
|----------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `target_reached`                                         | `not off_map and cell_exit and next_entry_point in agent_targets`, where `next_entry_point` is a plain parameter of `_candidate_speed`/`_candidate_distance` too, not only `_candidate_entry_points`.                                                                                                                                                                                                                                                                                                                                                                                | ✅ identical - one shared definition in all three, read directly from `next_entry_point` rather than by checking set membership on the already-resolved `candidate_entry_point`. Requiring `not off_map and cell_exit` structurally guarantees target-reached only fires for an agent genuinely completing a crossing into a target cell - not any other coincidence of position and target-set membership (e.g. an off-map agent's `initial_entry_point` happening to coincide with a target on the step it departs).                                                                                         |
| `action_invalid_on_rail`                                 | `transition_invalid and not off_map` - `transition_invalid` (Table 2a) narrowed to on-map context: a "rail" transition-validity check is only meaningful once the agent is actually on the rail. The off-map departure check (`required_action_invalid_or_not_required_or_no_movement` below) needs the raw `transition_invalid` instead, since `off_map` is true there by construction.                                                                                                                                                                                             | ✅ identical - a named local in all three, computed the same way from `transition_invalid`/`off_map`. Adding `not off_map` here doesn't change any branch's outcome for its other consumers below: `on_map_cell_transition`/`invalid_action_at_cell_exit`'s own branches already guarantee `not off_map` themselves - `on_map_cell_transition` explicitly (`not off_map and cell_exit and ...`), `invalid_action_at_cell_exit` via `action_invalid_on_rail` itself already being `False` off map (not via `cell_exit`, which is `True` off map - see Table 2a) - a semantic correction, not a behavior change. |
| `on_map_cell_transition`                                 | `not off_map and cell_exit and not action_invalid_on_rail` (entry_points only) - target_reached's on-map counterpart: a genuine crossing that isn't (also) a target arrival                                                                                                                                                                                                                                                                                                                                                                                                          | N/A - entry_points only; absorbed into speed/distance's own **Default** branch (see Table 1), which doesn't need to distinguish a genuine crossing from an ordinary mid-cell step                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `invalid_action_at_cell_exit`                            | `action_invalid_on_rail and cell_exit` (speed/distance name this; entry_points inlines the same conjunction into its Invalid action at cell exit branch)                                                                                                                                                                                                                                                                                                                                                                                                                             | ✅ same conjunction, using the same shared `cell_exit` everywhere                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ready_to_depart`                                        | `not done and (agent.earliest_departure <= elapsed_steps)` (entry_points only), uniformly for every `elapsed_steps` including the first call (`elapsed_steps == 1`) - an `earliest_departure` of `0` or `1` both resolve to "ready at the first opportunity" there, since there's no step 0 to have made them distinguishable. Composes `done` (Table 2a) - a `DONE` agent is never "ready to depart" regardless of `earliest_departure` timing, so this belongs here rather than in Table 2a. Every consumer below already applies its own `not done` guard at the branch, so including it here doesn't change any branch's outcome - it's a semantic correction, not a behavior change.                                                                                  | N/A - entry_points only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `required_action_invalid_or_not_required_or_no_movement` | `transition_invalid or not movement_action_given or not ready_to_depart` (entry_points only) - names *why* an off-map agent doesn't depart this step, as one of three disjoint cases (see below the table)                                                                                                                                                                                                                                                                                                                                                                           | N/A - entry_points only, used in place of `not map_entry` in its own off_map_no_departure branch (given `off_map`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `map_entry`                                              | `off_map and not required_action_invalid_or_not_required_or_no_movement` (entry_points only - speed/distance consume its *result*, `not stay_off_map`, rather than this richer predicate)                                                                                                                                                                                                                                                                                                                                                                                            | N/A - speed/distance's "map entry" condition is a downstream proxy for this, not the same expression                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `off_map_no_departure`                                   | `off_map and required_action_invalid_or_not_required_or_no_movement` (entry_points only); equivalently `off_map and not map_entry`. Core predicate, like `map_entry` above - `not done`/`not in_malfunction` are applied separately at the branch, not folded into this term. Names the branch this way: an agent that was already off map and, for one of `required_action_invalid_or_not_required_or_no_movement`'s three reasons, doesn't depart this step either                                                                                                                 | N/A - entry_points only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `stay_off_map`                                           | `candidate_entry_point is None` - a named local in `_candidate_speed`/`_candidate_distance` (used in their own map entry/stay off map branches in place of `candidate_entry_point is not None`/`is None`), consuming the already-resolved value rather than re-deriving it. `None` in exactly three of `_candidate_entry_points`' branches, all requiring `off_map`: **Done** (`done` - an already-removed agent stays removed), **Malfunction** (`in_malfunction ∧ ¬done` - `MALFUNCTION_OFF_MAP`), and **off_map_no_departure** (`¬done ∧ ¬in_malfunction ∧ off_map_no_departure`) | N/A for entry_points itself (it's the producer of `candidate_entry_point`, not a consumer of this named-local form of it). Distinct in meaning from `action_invalid_on_rail`: `stay_off_map` means "no departure happened this step" - broader than "the action was invalid" (see `required_action_invalid_or_not_required_or_no_movement` above for the full breakdown of just the third of its three producing branches).                                                                                                                                                                                   |
| `no_earlier_case_applies`                                | `¬done ∧ ¬target_reached ∧ ¬in_malfunction ∧ ¬off_map ∧ ¬invalid_action_at_cell_exit`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | ✅ same 5 exclusions in all three (speed names it explicitly; distance/entry_points reach the same set via elimination/fallthrough)                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

```
stay_off_map (candidate_entry_point is None)
⟺ off_map ∧ (
done                                              -- Done
∨ (¬done ∧ in_malfunction)                        -- Malfunction (MALFUNCTION_OFF_MAP)
∨ (¬done ∧ ¬in_malfunction ∧ off_map_no_departure) -- off_map_no_departure
)
```

`required_action_invalid_or_not_required_or_no_movement` breaks down into three disjoint cases - why an
off-map agent doesn't depart this step:

- **not yet ready to depart** (`¬ready_to_depart`) - state not yet `READY_TO_DEPART`, an action isn't
  required yet.
- **ready to depart but no moving action given** (`ready_to_depart ∧ ¬movement_action_given`) - e.g.
  `DO_NOTHING`, an action was required but a departure wasn't attempted.
- **a moving action given that doesn't resolve to a valid transition on the rail**
  (`ready_to_depart ∧ movement_action_given ∧ transition_invalid`) - an action was required,
  attempted, and invalid.

## Table 3 — distribute phase: candidate accepted or discarded

Tables 1, 2a and 2b are the *collect* phase: purely optimistic, pre-step-only derivations, computed for every agent before
any conflict is resolved. `RailEnv.step()`'s distribute phase then resolves conflicts via `MotionCheck`
(`resource_check = self.resource_check.check_resource(i_agent)`) and, per agent, either commits the already-computed
`candidate_entry_point`/`candidate_next_entry_point`/`candidate_speed`/`candidate_distance` as-is or falls back to a
discarded-candidate formula - this is `step()`'s (10a) POSITION UPDATE and (10b) SPEED_COUNTER UPDATE blocks.
`agent.old_entry_point` is this step's stable pre-step snapshot (captured before (10a) touches anything) of
`current_entry_point`, i.e. `off_map` (Table 2a) - (10b)'s own discarded-candidate branch reads it to tell
"was this agent on or off map going into this step" apart: `pass` (a true no-op, not just an unchanged-value
assignment) is only correct off map because the `speed_counter` invariant already holds `(None, None)` there, with
nothing to reset.

| Case                          | Condition                                                                                                                          | Speed (`speed_counter.speed`)                                                           | Distance (`speed_counter.distance`)                                                                                                                                                                | Entry points (`current_entry_point`, `next_entry_point`)                                                                                                                                                                          |
|-------------------------------|------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Accepted, ends up on map**  | `resource_check ∧ candidate_entry_point is not None ∧ ¬(remove_agents_at_target ∧ (done ∨ candidate_entry_point ∈ agent.targets))` | `candidate_speed`, accepted as-is                                                       | `candidate_distance`, accepted as-is                                                                                                                                                               | `(candidate_entry_point, candidate_next_entry_point)`, accepted as-is - covers every remaining case: a genuine crossing, a mid-cell no-op, map entry, and target reached while parked or `DONE` (`remove_agents_at_target=False`) |
| **Accepted, ends up off map** | `resource_check ∧ (candidate_entry_point is None ∨ (remove_agents_at_target ∧ (done ∨ candidate_entry_point ∈ agent.targets)))`    | `None` - forced (`_candidate_speed` itself never returns `None`, see its own docstring) | `candidate_distance`, accepted as-is - always `None` here by construction (Table 1's Target reached branch, when `remove_agents_at_target`, and its Stay off map branch both always return `None`) | `(None, None)` - either explicitly cleared (target reached and removed) or accepted as-is, since `candidate_entry_point`/`candidate_next_entry_point` are already `(None, None)` (stayed off map)                                 |
| **Denied on-map crossing**    | `¬resource_check ∧ ¬off_map`                                                                                                       | `0` - forced stop                                                                       | `distance_without_crossing(distance, speed)` - banked at the boundary, not credited with the (denied) crossing                                                                                     | unchanged - `(current_entry_point, next_entry_point)`, candidate discarded                                                                                                                                                        |
| **Denied map entry**          | `¬resource_check ∧ off_map`                                                                                                        | unchanged - already `None` (off-map invariant)                                          | unchanged - already `None`                                                                                                                                                                         | unchanged - already `(None, None)`. A true no-op: candidate discarded before ever leaving the map                                                                                                                                 |

**Accepted, ends up off map** merges two underlying branches (target reached and removed vs. stayed off map) that
have distinct conditions but always resolve to the identical `(None, None, (None, None))` triple: `_candidate_speed`
never returns `None` itself - it returns `0` for both underlying branches (Table 1) - so (10b) forces `None` in
both, overriding the already-computed `agent_transition_data.candidate_speed`, to match the invariant that a
removed/off-map agent's speed must be `None`. Distance needs no such forcing, since `_candidate_distance` already
returns `None` for both directly (Table 1).

## Table 4 — code ↔ paper mathematical notation

A separate write-up formalizes the collect/distribute phases in mathematical notation (superscript `t`/`t+1`
for pre-/post-step, subscript `i` for agent, `~` for an as-yet-unaccepted candidate). Tables 4a-4e map every
symbol it uses back to the code identifier (and, where one already exists, the row/term of Tables 1-3 above)
it denotes - a bridge between the two write-ups, not a replacement for either. "Notes" points back into this
file where the term is already documented, or says so explicitly when the paper introduces a symbol this file
doesn't otherwise name.

**Scope of the paper's position/done formulae (relevant to several "Notes" below):** the paper's collect-phase
position candidate and its `dones`/`\theta` bookkeeping only model `remove_agents_at_target=True` - a DONE
agent's position is always given as `(\bot,\bot)`, which only matches code when the agent has actually been
removed from the map. Under `remove_agents_at_target=False` (parked at target, never removed - Table 1's
Target reached row, Table 3's Accepted-ends-up-on-map row), a DONE agent's `current_entry_point` stays its
resolved target entry point, not `None`. The paper doesn't need a second branch for that case - it's simply
out of scope - so this is a scope note for L3 (keep the single `remove_agents_at_target=True`-shaped branch
when rewriting the LaTeX, not an error to "fix" by adding a branch), not a code/paper bug.

### Table 4a - base quantities

| Code                                                                 | Math notation                          | Notes                                                                                                          |
|-----------------------------------------------------------------------|-----------------------------------------|------------------------------------------------------------------------------------------------------------------|
| *(the RL policy/controller calling `env.step(action_dict)`)*           | `ctrl`                                  | Not a code identifier - the external caller supplying `action_dict`, outside `AbstractRailEnv` entirely            |
| `action` (param of all 3 `_candidate_` methods); `action_dict[agent.handle]` (in `collect()`) | `\action_i^t`                           | n/a - not in Tables 1-3 by this name, but every branch condition in Table 2a/2b is defined in terms of it        |
| `agent_transition_data.done` (equivalently `self.dones[i_agent]` after `handle_done_state()`) | `i \in dones^t`                          | Table 2a, `done`                                                                                                |
| `self.dones` (dict keyed by agent handle, plus `"__all__"`)            | `dones^t` (the set itself)               | New term - Table 2a/2b's `done` is the per-agent membership test, not the aggregate set                          |
| `agent.target_entry_point is not None`                                 | `\theta_i^t` (persistent target-reached flag) | New term - code has no separate flag distinct from `done`/`agent.target_entry_point`. Only matches under `remove_agents_at_target=True` (see the scope note above and Table 4e) - out of scope, not a bug, for L3 |
| `in_malfunction` (param); `agent.malfunction_handler.in_malfunction`   | `m_i^{t} \not= \bot`                     | Table 2a, `in_malfunction`. The original write-up's speed equation used `m_i^{t+1}` here while its position equation used `m_i^t` for this same shared parameter - `m_i^{t+1}` isn't defined until the distribute-phase malfunction-counter equation runs, so `m_i^t` (matching position's original convention) is correct; fixed in snippet.tex (L3) |
| `agent.malfunction_handler.malfunction_down_counter`                   | `m_i^t` (the counter value itself)       | New term - Table 2a only names the boolean `in_malfunction`, not the underlying counter                          |
| `off_map` (param); `current_entry_point is None`                       | `c_i^t = \bot`                           | Table 2a, `off_map`                                                                                             |
| `current_entry_point`, `next_entry_point` (params)                     | `c_i^t = (p, p')`                         | Table 2a, `off_map` definition context; Table 1 header                                                          |
| `agent.old_entry_point`                                                | (no separate symbol - reads the same `c_i^t = \bot` test as `off_map` above) | Table 3's own prose note; the pre-step snapshot (10b) reads to tell "off map going into this step" apart, distinct from `off_map` only as an implementation detail (captured before (10a) mutates anything), not in meaning |
| `cell_exit` (param of all 3 `_candidate_` methods); `speed is not None and speed > 0 and distance + speed >= SEGMENT_LENGTH` | `d_i^{t}+{s_i^{t}} \geq \lVert c_i^t \rVert` (the paper's inline boundary test - the `speed > 0` guard has no separate symbol) | Table 2a, `cell_exit` - shared, listed here rather than under one candidate method's own table (4b/4c/4d) |
| `target_reached` (param of all 3 `_candidate_` methods)                | `\tau(p',\action_i^t) \in targets_i` (combined with `\neg off\_map \wedge cell\_exit` per Table 2b) | Table 2b, `target_reached` - shared, see Table 4c's own Target reached row for `_candidate_entry_points`' richer from-scratch derivation |
| `transition_invalid` (named local in `collect()`; passed directly only to `_candidate_entry_points` - see Table 4c) | `\tau(p',\action_i^t) = \bot`             | Table 2a, `transition_invalid`                                                                                  |
| `action_invalid_on_rail` (param of `_candidate_entry_points` only)      | no separate symbol - the paper always uses the raw `\tau(p',\action_i^t)=\bot` test, without an on-map narrowing | Table 2b, `action_invalid_on_rail` - new term relative to the paper                                             |
| `invalid_action_at_cell_exit` (param of all 3 `_candidate_` methods; `action_invalid_on_rail and cell_exit`) | the paper's "(invalid action)" branch condition, `\tau(p',\action_i^t)=\bot \text{ and } d_i^t+s_i^t\geq\lVert c_i^t\rVert`, repeated inline in the speed/distance equations (see Table 4b/4d) rather than named | Table 2b, `invalid_action_at_cell_exit` - see Table 4c's own note: the paper's *position* equation has no corresponding branch at all (flag for L3) |
| `stay_off_map` (param of `_candidate_speed`/`_candidate_distance`; `candidate_entry_point is None`) | no separate symbol - the paper's position equation has a "stay off map" value case (`\tilde c_i^{t+1}=(\bot,\bot)`, via its map-entry branch's negation) rather than a named boolean reused by the other two equations | Table 2b, `stay_off_map`                                                                                        |
| `remove_agents_at_target` (param of `_candidate_distance`; constructor attr)                                    | implicit - see the scope note above; every paper branch that clears position/`\theta` to done assumes this is always `True` | Not in Tables 1-3 by a dedicated row - central to Table 3's two "Accepted" rows and the scope note above         |
| `speed` (param); `agent.speed_counter.speed`                           | `s_i^t`                                  | Table 2a, `stopped` (`speed == 0`) uses this                                                                    |
| `distance` (param); `agent.speed_counter.distance`                     | `d_i^t`                                  | Table 2a, `cell_exit` uses this                                                                                 |
| `SEGMENT_LENGTH` (`flatland.envs.step_utils.speed_counter`)            | `\lVert c_i^t \rVert`                    | Table 2a, `cell_exit` definition                                                                                |
| `stopped` (param); `speed == 0`                                        | `s_i^t = 0`                              | Table 2a, `stopped`                                                                                             |
| `agent_max_speed` (param); `agent.speed_counter.max_speed`             | `v_{\text{max},i}`                       | New term - not named in Tables 1-3, only used inline in their formula column                                    |
| `acceleration_delta` (constructor attr / param)                        | `\Delta{}v_a`                            | New term                                                                                                        |
| `braking_delta` (constructor attr / param)                             | `\Delta{}v_b`                            | New term                                                                                                        |
| `RailEnvActions.MOVE_LEFT` / `MOVE_FORWARD` / `MOVE_RIGHT`; `RailEnvActions.is_moving_action(action)` (`movement_action_given`) | `\action_i^t \in \{\text{L},\text{F},\text{R}\}` | Table 2b, `required_action_invalid_or_not_required_or_no_movement` uses `movement_action_given`                |
| `RailEnvActions.MOVE_FORWARD`                                          | `\action_i = \text{F}`                   | Table 1, "Acceleration or start moving"                                                                         |
| `RailEnvActions.STOP_MOVING`                                           | `\action_i = \text{S}`                   | Table 1, "Braking"                                                                                              |
| `initial_entry_point` (param); `agent.initial_entry_point`             | `p_{i,1,1}`                              | Table 1, "Map entry" row                                                                                        |
| `earliest_departure` (param); `agent.earliest_departure`               | `e_{i,1,1}`                              | Table 2b, `ready_to_depart`                                                                                     |
| `self._elapsed_steps + 1` (the step about to be taken)                 | `t+1`                                    | Table 2b, `ready_to_depart` (`elapsed_steps`)                                                                   |
| `agent.targets`                                                        | `targets_i`                              | Table 2b, `target_reached`                                                                                      |
| `candidate_entry_point_independent` (`self.rail.apply_action_independent(...)`) | `\tau(p', \action_i^t)` (transition function, general form) | Table 2a, `transition_invalid`                                                     |

### Table 4b - Step Collect Phase: speed candidate (`_candidate_speed`)

| Code                                                                 | Math notation                          | Notes                                             |
|-----------------------------------------------------------------------|------------------------------------------|-----------------------------------------------------|
| `candidate_speed`; `agent_transition_data.candidate_speed`             | `\tilde{s}_i^{t+1}`                       | Table 1, `_candidate_speed` column                |
| `target_reached`, `stay_off_map`, `cell_exit` (params)                  | see Table 4a - shared across all 3 `_candidate_` methods, not speed-specific | Table 4a                                          |
| `ZERO_FRACTION`, `invalid_action_at_cell_exit` branch                   | `0` elif `\tau(p',\action_i^t) = \bot` and `d_i^t+s_i^t \geq \lVert c_i^t \rVert` (invalid action) | Table 1, "Invalid action at cell exit" row (`_candidate_speed` column); Table 4a's `invalid_action_at_cell_exit` row |
| `SpeedCounter.speed_after_acceleration(speed, agent_max_speed, acceleration_delta)` | `\min\{s_i^{t}+\Delta{}v_a,v_{\text{max},i}\}` | referenced in `flatland/envs/step_utils/speed_counter.py`, not this file      |
| `_cap_speed(agent_max_speed, acceleration_delta)`                       | `\min\{\Delta{}v_a,v_{\text{max},i}\}`    | Table 1, "Map entry" row (`_candidate_speed` column) |
| `SpeedCounter.speed_after_braking(speed, braking_delta)`                | `\max\{s_i^{t}+\Delta{}v_b,0\}`           | referenced in `speed_counter.py`, not this file   |
| `speed` (unchanged, default/keep-moving-mid-cell branch)                | `s_i^{t}` (else branch)                  | Table 1, "Default"/"Stopped" rows                 |

### Table 4c - Step Collect Phase: position candidate (`_candidate_entry_points`)

| Code                                                                 | Math notation                          | Notes                                             |
|-----------------------------------------------------------------------|------------------------------------------|-----------------------------------------------------|
| `(candidate_entry_point, candidate_next_entry_point)` (return value)   | `\tilde{c}_i^{t+1}`                       | Table 1, `_candidate_entry_points` column         |
| `(current_entry_point, next_entry_point)` (unchanged, done branch)     | `(\bot,\bot)` (paper's Done row)          | Scoped to `remove_agents_at_target=True` (see the scope note above) - a DONE agent's `current_entry_point` is already `None` by then (set by `handle_done_state()` the step it first reached DONE), so "unchanged" and `(\bot,\bot)` coincide; out of scope, not a bug, under `remove_agents_at_target=False`. See Table 1's own Done row |
| `(initial_entry_point, candidate_entry_point_independent)`             | `(p_{i,1,1}, \tau(p_{i,1,1}, \action_i^t))` | Table 1, "Map entry" row                        |
| `(next_entry_point, candidate_entry_point_independent)`, target-reached branch | `(\bot,\bot)` (paper's Target reached row) | Scoped to `remove_agents_at_target=True` (see the scope note above) - `_candidate_entry_points` itself always returns the resolved position here, and the `(None, None)` clearing only happens in the distribute phase (Table 3) when `remove_agents_at_target`; out of scope, not a bug, under `remove_agents_at_target=False`. See Table 1's Target reached row |
| `(next_entry_point, candidate_entry_point_independent)`, on-map cell transition branch | `(p',\tau(p',\action_i^t))`      | Table 1, "On-map cell transition" row             |
| `(current_entry_point, next_entry_point)` (unchanged, invalid action at cell exit branch) | no corresponding branch - see the gap noted below | Table 1, "Invalid action at cell exit" row (`_candidate_entry_points` column); Table 4a's `invalid_action_at_cell_exit`/`transition_invalid` rows |
| `(current_entry_point, next_entry_point)` (unchanged, default/stay branches) | `c_i^{t}` (else branch)             | Table 1, "Default" row                            |

Gap (for L3, distinct from the `remove_agents_at_target` scope note above): the paper's position equation has no
"invalid action" branch at all. Its "cell transition" branch (`(p',\tau(p',\action_i^t))` when
`c_i^t\not=(\bot,\bot)` and `d_i^t+\tilde s_i^{t+1}\geq\lVert c_i^t\rVert`) doesn't exclude
`\tau(p',\action_i^t)=\bot` the way `on_map_cell_transition`'s own `not action_invalid_on_rail` does in code
(Table 2b) - so as written, a denied/invalid transition at a cell boundary would fall into "cell transition" and
set the next entry point to `\bot` instead of being denied and keeping `(current_entry_point, next_entry_point)`
unchanged, as code's Invalid action at cell exit branch does (Table 1). The speed and distance equations both
already have their own explicit "(invalid action)" branch (see Table 4a/4b/4d) - only the position equation is
missing one.

### Table 4d - Step Collect Phase: distance candidate (`_candidate_distance`)

| Code                                                                 | Math notation                          | Notes                                             |
|-----------------------------------------------------------------------|------------------------------------------|-----------------------------------------------------|
| `candidate_distance`; `agent_transition_data.candidate_distance`       | `\tilde{d}_i^{t+1}`                       | Table 1, `_candidate_distance` column             |
| `target_reached`, `stay_off_map`, `cell_exit` (params)                  | see Table 4a - shared across all 3 `_candidate_` methods, not distance-specific | Table 4a                                          |
| `remove_agents_at_target` (param)                                       | implicit - see the scope note above       | Table 4a's `remove_agents_at_target` row; Table 1's Target reached row is the one branch that actually reads it |
| `SpeedCounter.distance_without_crossing(distance, speed)`, `invalid_action_at_cell_exit` branch | `\min(d_i^{t} + s_i^t, \lVert c_i^t \rVert)` (the paper's "(invalid action)" branch) | referenced in `speed_counter.py`, not this file; Table 1's Done/Invalid-action rows both return this; Table 4a's `invalid_action_at_cell_exit` row |
| `SpeedCounter.distance_after_crossing(distance, speed)`                 | `d_i^{t} + s_i^t \mod \lVert c_i^t \rVert`  | referenced in `speed_counter.py`, not this file; Table 1's Default row                              |

Mismatch (unrelated to the `remove_agents_at_target` scope note above, for L3): the paper's distance formula has
only two cases - invalid action (`\min(d+s, \lVert c \rVert)`) and "valid action" (`d+s \mod \lVert c \rVert`),
with a comment claiming the valid-action case "covers malfunction, done, target reached, off map" too. None of
those four actually reduce to `d+s \mod \lVert c \rVert` in code (Table 1): Done and Invalid action both return
`distance_without_crossing` (`\min(d+s, \lVert c \rVert)` - the paper's *other* branch, not this one);
Malfunction returns `distance` unchanged (no `+s` at all); Map entry returns `0`; Target reached returns either
`None` (`remove_agents_at_target`) or `distance_without_crossing`. The paper's two-case split doesn't yet cover
these the way its own comment claims.

### Table 4e - Step Distribute Phase: accept/reject

| Code                                                                 | Math notation                          | Notes                                             |
|-----------------------------------------------------------------------|------------------------------------------|-----------------------------------------------------|
| `resource_check`; `self.resource_check.check_resource(i_agent)`        | `\mu(\tilde{c}_i^{t+1}, \action_i^t) = \top` | Table 3, `Condition` column                       |
| `agent.speed_counter.speed` (post `.set()`)                             | `s_i^{t+1}`                               | Table 3, `Speed` column                           |
| `(agent.current_entry_point, agent.next_entry_point)` (post-update)     | `c_i^{t+1}`                               | Table 3, `Entry points` column                    |
| `agent.speed_counter.distance` (post `.set()`)                          | `d_i^{t+1}`                               | Table 3, `Distance` column                        |
| — (no code equivalent - see Table 4a's `\theta_i^t` row)                | `\theta_i^{t+1}`                          | Scoped to `remove_agents_at_target=True` (see the scope note above) - the paper derives `dones^{t+1}` from a `\theta` transition; code just re-reads `agent.target_entry_point is not None` directly (Table 2a's `done`), there is no separate persistent flag or transition rule. Out of scope, not a bug, under `remove_agents_at_target=False` |
| `self.dones[agent.handle] = True` (in `handle_done_state()`)            | `dones^{t+1}`                             | Table 2a, `done` (per-agent); see Table 4a for the aggregate-set caveat |
| `MalfunctionHandler.update_counter()` (`if counter > 0: counter -= 1`, else stays `0`) | `m_i^{t+1}` (`\bot` if `m_i^t=\bot` or `m_i^t=1` else `m_i^t-1`, treating `\bot` as `0`) | New term - not in Tables 1-3, which only ever read the derived boolean `in_malfunction`, never the raw counter (see Table 4a's `m_i^t` row) |
| `ZERO_FRACTION` (forced, on-map candidate discarded)                    | `0` (`s_i^{t+1}` else branch)              | Table 3, "Denied on-map crossing" row (`Speed` column)                          |
| `SpeedCounter.distance_without_crossing(agent_transition_data.distance, agent_transition_data.speed)` (on-map candidate discarded) | `\min(\tilde d_i^{t} + \tilde s_i^t, \lVert c_i^t \rVert)` (`d_i^{t+1}` else branch) | Table 3, "Denied on-map crossing" row (`Distance` column) - N.B. the paper's else branch reads `\tilde d_i^{t}/\tilde s_i^t` (candidates from the *previous* step, no tilde-superscript bump), likely a `t` vs. `t+1` typo relative to the rest of the equation set - flag for L3 |
| `(current_entry_point, next_entry_point)` (unchanged, resource check denied) | `c_i^{t}` (`c_i^{t+1}` else branch)         | Table 3, "Denied on-map crossing"/"Denied map entry" rows (`Entry points` column) |
